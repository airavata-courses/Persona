kind: Service
apiVersion: v1
metadata:
  name: userservice-k8s-mysql
  labels:
    name: userservice-k8s-mysql
spec:
  ports:
    - nodePort: 30163 
      port: 2222      
      targetPort: 2222  
      protocol: TCP
  selector:           
    app: userservice-k8s-mysql
  type: NodePort       

---
apiVersion: apps/v1 
kind: Deployment    
metadata:              
  name: userservice-k8s-mysql
spec:                
  selector:         
    matchLabels:
      app: userservice-k8s-mysql
  replicas: 3        
  template:
    metadata:
      labels:        
        app: userservice-k8s-mysql
    spec:
      containers:
        - name: userservice-k8s-mysql
          image: personateam/user-service-mysql:latest
          ports:
            - containerPort: 2222                
          env:   # Setting Enviornmental Variables
          - name: DB_HOST   # Setting Database host address from configMap
            valueFrom: 
              configMapKeyRef:
                name: db-conf  # name of configMap
                key: host
          - name: DB_NAME  # Setting Database name from configMap
            valueFrom:
              configMapKeyRef:
                name: db-conf 
                key: name
          - name: DB_USERNAME  # Setting Database username from Secret
            valueFrom:
              secretKeyRef:
                name: db-credentials # Secret Name
                key: username
          - name: DB_PASSWORD # Setting Database password from Secret
            valueFrom:
              secretKeyRef:
                name: db-credentials
                key: password     
        